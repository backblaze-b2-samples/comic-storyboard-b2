"""Genblaze repository layer.

This is the ONLY package in the sample that imports ``genblaze_*``. Everything
above it (routes, schemas, config, story_breaker, strip) is Genblaze-agnostic.
The structural test in ``tests/test_structure.py`` enforces that boundary.
"""
